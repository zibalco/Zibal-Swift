// swift-tools-version:4.0
import PackageDescription

let package = Package(
	name: "Zibal",
	products: [
		.library(
			name: "Zibal",
			targets: ["Zibal"]),
		],
	dependencies: [
	],
	targets: [
		.target(
			name: "Zibal",
			dependencies: [])
	]
)
